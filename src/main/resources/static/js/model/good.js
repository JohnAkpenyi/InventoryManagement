export default class Good {
    constructor(name, amount, weightPerUnit, inventoryId, sectionId) {
      this.name = name;
      this.amount = amount;
      this.weightPerUnit = weightPerUnit;
      this.inventoryId = inventoryId;
      this.sectionId = sectionId;
    }

  // class methods
}