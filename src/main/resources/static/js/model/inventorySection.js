export default class InventorySection {
    constructor(name, description, inventoryId) {
      this.name = name;
      this.description = description;
      this.inventoryId = inventoryId;
    }

  // class methods
}